---
name: interpret
description: >
  분석 결과를 해석합니다. 가설 검증 판단(지지/기각), 주요 인사이트 도출, 
  추가 분석 방향 제안을 합니다. "결과 해석", "이 수치 어떻게 봐야 해?", 
  "분석 결과 정리", "인사이트" 등의 요청 시 사용.
---

# /interpret — 분석 결과 해석

분석 결과를 해석하고, 가설 검증 여부를 판단하며, 추가 분석 방향을 제안합니다.

## 실행 흐름

### 1단계: 정보 수집
AskUserQuestion을 사용하여 확인:

1. **분석 결과를 어떻게 공유할 건가요?**
   - 파일 경로 (.ipynb, .py, .csv)
   - 직접 수치를 텍스트로 공유
   - 스크린샷 (그래프 이미지)
2. **원래 가설은 무엇이었나요?** (분석 설계서 경로 또는 직접 설명)
3. **비즈니스 맥락이 있나요?** (기능 출시일, 프로모션 기간, 외부 이벤트 등)
4. **특별히 궁금한 포인트가 있나요?**

### 2단계: 결과 해석
`result-interpreter` 에이전트를 호출:

```
Agent(subagent_type="result-interpreter", prompt="[분석 결과 + 가설 + 비즈니스 맥락]")
```

해석에 포함:
- 핵심 수치 요약 (표)
- 가설 검증 결과 (✅ 지지 / ❌ 기각 / ⚠️ 부분적 지지)
- 주요 인사이트
- 주의사항 (표본 크기, 외부 변수 등)
- 추가 분석 제안 (우선순위별)

### 3단계: 해석 결과 검토
- 사용자에게 해석 결과를 보여주고 피드백 수집
- 추가 분석이 필요하면 방향 안내

### 4단계: 후속 안내
- **추가 코드 수정** → `/analyze`
- **시각화 추가** → `/viz`
- **보고서 작성** → `/report`

## 주의사항
- **데이터에 없는 내용을 추측하지 말 것.** 추가 확인이 필요하면 추가 분석으로 연결.
- 인과관계와 상관관계를 구분.
- 해석의 **확신 수준**을 명시 (강한 근거 vs 추가 확인 필요).
